export interface ProgramItem {
  id: number;
  title: string;
  performer: string;
  description: string;
  time: string;
}

export interface GalleryItem {
  id: number;
  src: string;
  alt: string;
}